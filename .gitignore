# Environment — secrets live here
.env
.env.local
.env.*.local

# Compiled Go binaries (root-level builds)
/api
/bulkimport
/personalizer

# Standard Go build output
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
/bin/
/dist/

# GeoNames data — allCountries.txt is 1.7GB, never commit
data/*.txt
data/*.zip
data/*.csv
data/*.tsv
# keep the placeholder
!data/.gitkeep

# Go workspace / vendor
vendor/
go.work
go.work.sum

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Docker volumes / local overrides
docker-compose.override.yml

# Test coverage
coverage.out
coverage.html

# Demo GIF — large binary, regenerate with: vhs demo/demo.tape
# demo/demo.gif

# Kubernetes secrets — never commit real credentials
k8s/secrets.yaml

# Claude Code internal
.claude/
